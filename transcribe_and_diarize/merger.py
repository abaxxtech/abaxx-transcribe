from __future__ import annotations

class Merger:
    def merge(self, transcript_data: dict, diarization_data: dict) -> list:
        raise NotImplementedError("Merger.merge() — implemented in task-012")
